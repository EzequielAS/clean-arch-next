describe('aplication flow validation', () => {
	// beforeEach(() => {
	//   cy.visit('http://localhost:3000/home')
	// })
	// it('must contain 20 anchor elements on Home', () => {
	//   cy.get('a').should('have.length', 20)
	// })
	// it('should be able to navigate to movie details', () => {
	//   cy.get('a').first().click()
	// })
	// it('must contain a content on Movie Details', () => {
	//   cy.get('a').last().click()
	//   cy.get('img').should('have.a.attr', 'src')
	//   cy.get('h2').should('not.be.empty')
	//   cy.get('p').should('not.be.empty')
	// })
})
